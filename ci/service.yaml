apiVersion: v1
kind: Service
metadata:
  name: lumen
  labels:
    app: lumen
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: lumen
