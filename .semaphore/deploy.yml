version: v1.0
name: Deploy
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Deploy'
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: 'Deploy'
          commands:
            - pwd
            - docker images
            - ./ci/deploy2.sh
